<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.rongcapital.mkt.dao.WechatQrcodeFocusDao">

	<!-- 所有查询列 -->
	<sql id="QUERY_COLUMN_LIST">
		<![CDATA[ id as id,qrcode_id as qrcodeId,wx_name as wxName,ch_code as chCode,openid as openid,focus_datetime as focusDatetime,unfocus_datetime as unfocusDatetime]]>
	</sql>

	<!-- 查询列来源表-->
	<sql id="QUERY_FROM_TABLE"><![CDATA[FROM wechat_qrcode_focus]]></sql>
	
	<!-- 全部条件 -->
	<sql id="QUERY_WHERE_CLAUSE">
		<where>
		    	<if test="id != null"><![CDATA[ AND id = #{id}]]></if>
		    	<if test="qrcodeId != null"><![CDATA[ AND qrcode_id = #{qrcodeId}]]></if>
		    	<if test="wxName != null"><![CDATA[ AND wx_name = #{wxName}]]></if>
		    	<if test="chCode != null"><![CDATA[ AND ch_code = #{chCode}]]></if>
		    	<if test="openid != null"><![CDATA[ AND openid = #{openid}]]></if>
		    	<if test="focusDatetime != null"><![CDATA[ AND focus_datetime = #{focusDatetime}]]></if>
		    	<if test="unfocusDatetime != null"><![CDATA[ AND unfocus_datetime = #{unfocusDatetime}]]></if>
		</where>
	</sql>
	
	<!-- 智能排序与分页 -->
	<sql id="QUERY_ORDER_LIMIT_CONDTION">
		<if test="orderField != null and orderFieldType != null"><![CDATA[ORDER BY ${orderField} ${orderFieldType}]]></if>
		<if test="startIndex != null and startIndex &gt;= 0 and pageSize != null and pageSize &gt; 0"><![CDATA[LIMIT #{startIndex},#{pageSize}]]></if>	
	</sql>

	<!-- 更新列字段,只要不为NULL则更新,除开主键列 -->
	<sql id="UPDATE_COLUMN_SET">
		<set>
		    	<if test="qrcodeId != null"><![CDATA[qrcode_id = #{qrcodeId},]]></if>
		    	<if test="wxName != null"><![CDATA[wx_name = #{wxName},]]></if>
		    	<if test="chCode != null"><![CDATA[ch_code = #{chCode},]]></if>
		    	<if test="openid != null"><![CDATA[openid = #{openid},]]></if>
		    	<if test="focusDatetime != null"><![CDATA[focus_datetime = #{focusDatetime},]]></if>
		    	<if test="unfocusDatetime != null"><![CDATA[unfocus_datetime = #{unfocusDatetime}]]></if>
		</set>
	</sql>

	<!-- 插入action记录 -->
	<insert id="insert" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" useGeneratedKeys="true" keyProperty="id">
		<![CDATA[
			INSERT INTO wechat_qrcode_focus (qrcode_id,wx_name,ch_code,openid,focus_datetime,unfocus_datetime)
			VALUES
			(
			  #{qrcodeId},
			  #{wxName},
			  #{chCode},
			  #{openid},
			  #{focusDatetime},
			  #{unfocusDatetime}
			)
		]]>
	</insert>
	
	<!-- 返回插入的编号,在事务开启状态下有效 -->
	<select id="lastSequence" resultType="int"><![CDATA[SELECT LAST_INSERT_ID() AS id]]></select>

	<!-- 修改记录通过主键 -->
	<update id="updateById" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus">
		<![CDATA[UPDATE wechat_qrcode_focus]]>
		<include refid="UPDATE_COLUMN_SET"/>
		<![CDATA[WHERE id = #{id}]]>
	</update>

	<!-- 查询,通过主键IN(array) -->
	<select id="selectListByIdList" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="cn.rongcapital.mkt.po.WechatQrcodeFocus">
		<![CDATA[SELECT]]>
		<include refid="QUERY_COLUMN_LIST"/>
		<include refid="QUERY_FROM_TABLE"/>
		<![CDATA[WHERE id IN]]>
		<foreach collection="list" item="id" open="(" separator="," close=")">
			<![CDATA[#{id}]]>
		</foreach>
	</select>

	<!-- 查询,通过条件 -->
	<select id="selectList" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="cn.rongcapital.mkt.po.WechatQrcodeFocus">
		<![CDATA[SELECT]]>
		<include refid="QUERY_COLUMN_LIST"/>
		<include refid="QUERY_FROM_TABLE"/>
		<include refid="QUERY_WHERE_CLAUSE"/>
		<include refid="QUERY_ORDER_LIMIT_CONDTION"/>
	</select>

	<!-- 总数查询,通过条件 -->
	<select id="selectListCount" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="int">
		<![CDATA[SELECT COUNT(id) AS dataCount]]>
		<include refid="QUERY_FROM_TABLE"/>
		<include refid="QUERY_WHERE_CLAUSE"/>
	</select>
	
	<!-- 其它SQL语句 -->
	<select id="selectTheEarliestFocus" resultType="cn.rongcapital.mkt.po.WechatQrcodeFocus">
		<![CDATA[SELECT]]>
		<include refid="QUERY_COLUMN_LIST"/>
		<include refid="QUERY_FROM_TABLE"/>
	    ORDER BY focus_datetime
	</select>
	
	<!-- 统计微信二维码关注数量和取消关注数量 -->
	<select id="getFocusOrUnFocusCount" parameterType="java.util.Map" resultType="java.lang.Integer">
		SELECT
			COUNT(1)
		FROM
			wechat_qrcode_focus
		WHERE
		<if test="fieldName != null and fieldName == 'focus_datetime' and searchDate != null ">
			<![CDATA[ DATE_FORMAT(focus_datetime,'%Y-%m-%d') = #{searchDate} ]]>
		</if>
		<if test="fieldName != null and fieldName == 'unfocus_datetime' and searchDate != null ">
			<![CDATA[ DATE_FORMAT(unfocus_datetime,'%Y-%m-%d') = #{searchDate} ]]>
		</if>
		<if test="fieldName != null and fieldName == 'focus_datetime' and searchHours != null ">
			<![CDATA[ DATE_FORMAT(focus_datetime,'%Y-%m-%d %H') = #{searchHours} ]]>
		</if>
		<if test="fieldName != null and fieldName == 'unfocus_datetime' and searchHours != null ">
			<![CDATA[ DATE_FORMAT(unfocus_datetime,'%Y-%m-%d %H') = #{searchHours} ]]>
		</if>
		AND ch_code = #{chCode}
		AND wx_name = #{wxName}
		AND #{fieldName} IS NOT NULL
	</select>
	
	
	<!-- 获取总浏览次数 -->
	<select id="getTotalFocus" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="java.util.Map">
		<![CDATA[SELECT qrcode_id as qrcodeId, COUNT(id) as value]]>
		<include refid="QUERY_FROM_TABLE"/>
		<where>
		    	<if test="wxName != null"><![CDATA[ AND wx_name = #{wxName}]]></if>
		    	<if test="chCode != null"><![CDATA[ AND ch_code = #{chCode}]]></if>
		</where>
		GROUP BY qrcode_id
		<include refid="QUERY_ORDER_LIMIT_CONDTION"/>
	</select>
	
	<!-- 获取新关注的信息 -->
	<select id="getNewFocus" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="java.util.Map">
		<![CDATA[SELECT qrcode_id as qrcodeId, COUNT(id) as value]]>
		<include refid="QUERY_FROM_TABLE"/>
		<where>
				and focus_datetime between #{focusDatetime} and #{unfocusDatetime}
		    	<if test="wxName != null"><![CDATA[ AND wx_name = #{wxName}]]></if>
		    	<if test="chCode != null"><![CDATA[ AND ch_code = #{chCode}]]></if>
		</where>
		GROUP BY qrcode_id
		<include refid="QUERY_ORDER_LIMIT_CONDTION"/>
	</select>
	
	<!-- 获取流失关注的信息 -->
	<select id="getLostFocus" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="java.util.Map">
		<![CDATA[SELECT qrcode_id as qrcodeId, COUNT(id) as value]]>
		<include refid="QUERY_FROM_TABLE"/>
		<where>
				and unfocus_datetime between #{focusDatetime} and #{unfocusDatetime}
		    	<if test="wxName != null"><![CDATA[ AND wx_name = #{wxName}]]></if>
		    	<if test="chCode != null"><![CDATA[ AND ch_code = #{chCode}]]></if>
		</where>
		GROUP BY qrcode_id
		<include refid="QUERY_ORDER_LIMIT_CONDTION"/>
	</select>
	
	<!-- 获取最大浏览次数 -->
	<select id="getAmountFocusMax" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="java.util.Map">
		<![CDATA[SELECT DATE_FORMAT(focus_datetime,'%Y-%m-%d') as time, count(id) as value]]>
		<include refid="QUERY_FROM_TABLE"/>
		<where>
		    	<if test="wxName != null"><![CDATA[ AND wx_name = #{wxName}]]></if>
		    	<if test="chCode != null"><![CDATA[ AND ch_code = #{chCode}]]></if>
		</where>
		<![CDATA[GROUP BY time order by value desc limit 1]]>
	</select>
	
	<!-- 获取最大新关注的信息 -->
	<select id="getNewFocusMax" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="java.util.Map">
		<![CDATA[SELECT DATE_FORMAT(focus_datetime,'%Y-%m-%d') as time, count(id) as value]]>
		<include refid="QUERY_FROM_TABLE"/>
		<where>
				and focus_datetime between #{focusDatetime} and #{unfocusDatetime}
		    	<if test="wxName != null"><![CDATA[ AND wx_name = #{wxName}]]></if>
		    	<if test="chCode != null"><![CDATA[ AND ch_code = #{chCode}]]></if>
		</where>
		<![CDATA[GROUP BY time order by value desc limit 1]]>
	</select>
	
	<!-- 获取最大净增关注的信息 -->
	<select id="getAddFocusMax" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="java.util.Map">
		<![CDATA[SELECT  newFocusData.newFocusTime as time, (newFocusData.newFocusValue - lostFocusData.lostFocusValue) value]]>
		<![CDATA[from]]>
		
		(<![CDATA[select DATE_FORMAT(focus_datetime,'%Y-%m-%d') as newFocusTime, count(id) as newFocusValue ]]>
		<include refid="QUERY_FROM_TABLE"/>
		<where>
				<![CDATA[and focus_datetime between #{focusDatetime} and #{unfocusDatetime}]]>
		    	<if test="wxName != null"><![CDATA[ AND wx_name = #{wxName}]]></if>
		    	<if test="chCode != null"><![CDATA[ AND ch_code = #{chCode}]]></if>
		</where>
		<![CDATA[GROUP BY newFocusTime]]>) <![CDATA[as newFocusData]]>
		
		<![CDATA[JOIN]]>
		
		(<![CDATA[SELECT DATE_FORMAT(focus_datetime,'%Y-%m-%d') as lostFocusTime, count(id) as lostFocusValue]]>
		<include refid="QUERY_FROM_TABLE"/>
		<where>
				<![CDATA[and unfocus_datetime between #{focusDatetime} and #{unfocusDatetime}]]>
		    	<if test="wxName != null"><![CDATA[ AND wx_name = #{wxName}]]></if>
		    	<if test="chCode != null"><![CDATA[ AND ch_code = #{chCode}]]></if>
		</where>
		<![CDATA[GROUP BY lostFocusTime]]>)  <![CDATA[as lostFocusData]]>
		
		<![CDATA[on newFocusData.newFocusTime = lostFocusData.lostFocusTime]]>
		<![CDATA[order by value desc limit 1]]>
		
	</select>
	
	<!-- 获取最大流失关注的信息 -->
	<select id="getLostFocusMax" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="java.util.Map">
		<![CDATA[SELECT DATE_FORMAT(focus_datetime,'%Y-%m-%d') as time, count(id) as value]]>
		<include refid="QUERY_FROM_TABLE"/>
		<where>
				and unfocus_datetime between #{focusDatetime} and #{unfocusDatetime}
		    	<if test="wxName != null"><![CDATA[ AND wx_name = #{wxName}]]></if>
		    	<if test="chCode != null"><![CDATA[ AND ch_code = #{chCode}]]></if>
		</where>
		<![CDATA[GROUP BY time order by value desc limit 1]]>
	</select>
	
	<!-- 根据微信名和渠道号获取二维码id -->
	<select id="getQrcodeIdList" parameterType="cn.rongcapital.mkt.po.WechatQrcodeFocus" resultType="java.lang.String">
		<![CDATA[SELECT qrcode_id]]>
		<include refid="QUERY_FROM_TABLE"/>
		<where>
			    	<if test="wxName != null"><![CDATA[ AND wx_name = #{wxName}]]></if>
			    	<if test="chCode != null"><![CDATA[ AND ch_code = #{chCode}]]></if>
			    	qrcode_id IS NOT NULL
		</where>
		GROUP BY qrcode_id
	</select>
</mapper>